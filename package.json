{
    "name": "esith-pfe-platform-root",
    "version": "1.0.0",
    "scripts": {
        "build": "npm install --prefix server && npm run build --prefix server && npm install --prefix client && npm run build --prefix client",
        "start": "node server/dist/index.js"
    }
}